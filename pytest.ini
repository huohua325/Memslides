[pytest]
pythonpath = src
testpaths = tests
norecursedirs =
    .cache
    .history
    .memory
    .playwright-browsers
    archive
    build
    build_legacy_*
    dist
    dist_legacy_*
    experiment_outputs
    logs
    release
    node_modules
    __pycache__
